{
  "name": "timeout-fetch-retry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf ./dist && npx tsc",
    "start": "node ./dist/index.js",
    "start:retry": "node ./dist/index.js retry",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-fetch": "^3.1.6",
    "fetch-retry-ts": "^1.1.25"
  },
  "devDependencies": {
    "@types/node": "^20.3.2",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
